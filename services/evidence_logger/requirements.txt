fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# Logging / JSON utilities (stdlib + FastAPI are usually enough)

# Testing (used from root test env; optional per-service install)
pytest>=8.0
httpx>=0.27
